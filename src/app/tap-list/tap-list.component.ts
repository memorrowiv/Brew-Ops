import { Component } from '@angular/core';

@Component({
  selector: 'app-tap-list',
  standalone: true,
  templateUrl: './tap-list.component.html',
  styleUrls: ['./tap-list.component.css'],
})
export class TapListComponent {
  title = 'Tap List';
  // This will hold the tap list logic later
}
